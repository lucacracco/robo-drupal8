# This is an example for another site (multisite drupal).

# Drupal.
drupal:
  site_name: example
  account:
    mail: example@localhost
    username: example
    password: example
  db:
    database: example
