# This is an example for another site (multisite drupal).

# Drupal.
drupal:
  site_name: default
  account:
    mail: default@localhost
    username: default
    password: default
  databases:
    default:
      default:
        database: drupal
        username: drupal
        password: drupal
        host: mariadb
        port: 3306
        driver: mysql
