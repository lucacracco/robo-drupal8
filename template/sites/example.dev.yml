# This is an example for another site (multisite drupal) override for environment 'dev'.

# Drupal.
drupal:
  site_name: example
  account:
    mail: example@localhost
    username: example
    password: example
  db:
    database: dev_example
    host: localhost
